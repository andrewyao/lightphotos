/target
/LightPhotos.app
# trunk's wasm build output (index.html/Trunk.toml are the real source)
/dist
# roborev snapshots
/.roborev/
/.claude/
# rawler RAW-decode crate, materialized + patched per-clone by
# scripts/setup-vendor-rawler.sh (run it once before any cargo command)
/vendor/
